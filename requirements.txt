# Core runtime
PySide6>=6.6
reportlab>=4.0

# Optional archive formats (graceful degradation if absent)
py7zr>=0.21   # enables .7z extraction
rarfile>=4.0  # enables .rar extraction (also needs an 'unrar' binary on PATH)

# Development / build
pytest>=8.0
pyinstaller>=6.0
